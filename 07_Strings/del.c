
#include <stdio.h>

int main(){
    int count=0;
    char word[100],arr[10000000];
    printf("Enter the text: ");
    gets(arr);
    printf("Enter word to count: ");
    gets(word);
    for(int i=0;arr[i]!='\0';i++){
        if(arr[i]==)
        count++;
    }
    printf("Number of %c in %s is: %d",c,arr,count);
    return 0;
}